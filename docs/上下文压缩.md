# 上下文压缩(新会话启动必读)

> **用途**:每次开新对话,先把本文件喂给 AI(或让它读本文件),即可恢复项目全部关键状态与进度。AI 应默认:本文件即"当前事实",以下功能已完成,不要建议重做。
> 其他文档:`docs/design.md`(总方案)、`docs/真机验收用例清单.md`、`docs/分工与角色交接文档.md`(团队任务书+角色提示词)、`docs/进度汇报.md`(给老师版)、`docs/第三方调试手册.md`。

---

## 0. 一句话
华为竞赛命题"基于鸿蒙原生智能的多模态个人助理",装在手机/平板(后续 PC),全 ArkTS。核心卖点:**端侧优先、回答带出处、多模态+多智能体编排**。仓库 `D:\MyCode\DevEcoStudioProjects\Harmony_Agent`(git 干净,最新 commit `31a3bce`)。

## 1. 已完成(勿重做)
| 里程碑 | 状态 |
|---|---|
| D1-D4 知识库浅层 | ✅ 架构/文本对话/语音代码/知识库(导入/切块/检索/引用溯源/文档预览/恢复去重/敏感词) |
| D6 编排 L1 | ✅ 规则复合链:复合指令拆 Document→Schedule(`76e3e17`,ChainPlanner) |
| D6 编排 L2 | ✅ Agentic:DeepSeek function calling 3 工具自主编排(`ba8383f`,开关 `ENABLE_AGENTIC` 默认关) |
| D7.1 一多双栏 | ✅ 平板/折叠双栏、手机单栏(非 phone + ≥600vp) |
| D7 跨端骨架 | ✅ 应用接续:continuable+onContinue+会话JSON+冷热启恢复(`a927a59`,SessionCodec) |
| D8 冷启动+流式 | ✅ 权限改运行时+PerfLaunch 打点;LLM 流式(DeepSeek SSE+Mock 打字机,`40320f0`) |
| 多模态输入 | ✅ 图片多选(≤9)+文字组合消息、多图 OCR 汇总、消息渲染图(`af6ab43`) |
| UI 修复 | ✅ 键盘避让 RESIZE(`8e514af`);气泡溢出:wordBreak+constraintSize 内容自适应短贴长封顶(`31a3bce`;**用户已确认 OK**) |
| 单测 | ✅ **50 条全绿**(LocalUnit 15 + AgentUnit 35),本地入口 `entry/src/test/List.test.ets` |

## 2. 关键配置/事实
- `LlmConfig.ets`:ENABLE_DEEPSEEK=false、ENABLE_AGENTIC=false(**默认 Mock 安全;真机联调/演示真实 LLM 时才开 true**)
- DeepSeek API Key 明文在 LlmConfig.ets(**分发/给第三方前必须处理**)
- 知识库:**自研通道可演示**;官方 DataAugmentationKit 仅 PC/2in1 能加工(4b),手机端官方检索(4a)需先有 4b 产物 —— 无 PC/2in1 则整体挂起,演示走自研(命中扣分点已记录)
- 模拟器:仅 Mock 全流程;语音/OCR/日历/DataAugmentationKit/应用接续 均需真机
- UI shell(devecocli ui)在模拟器点输入框会偶发把 App 点到后台 → 无法可靠目视验证;UI 结果以用户肉眼确认为准

## 3. 剩余缺口(按优先级)
1. **真机项**(验收官可随时开跑,照 `真机验收用例清单.md`):D3 语音、OCR/日历(TC-D6-5/6)、双栏截图、性能实测(冷启动≤3s/首字≤2s)、跨端接续联调
2. **#3 示例知识库语料**:素材官产 5~8 篇(模板在交接文档 §3.3;现仅 3 篇小示例)
3. **#4a/b 官方 Kit**:卡 PC/2in1(资源官落实)
4. **计划书 #9**:等真机数据回填;素材见 `进度汇报.md`/交接文档 §3.5
5. L2 Agentic 与跨端骨架需 **DeepSeek 真连 + 双真机组网**验证

## 4. 团队与分工(7 人)
| 角色 | 现主线 |
|---|---|
| 编码核心(用户+AI) | Agent 编排 L1/L2 已完成;剩穿插:#4a(等设备)、真机联调配合、代码骨架维护 |
| 真机验收官 ×2 | 手机/平板各一:跑 #5/#6/#8 等真机用例,报问题单(不改代码) |
| 素材官 | #3 示例语料(可最先开工) |
| 资源/规则官 | PC/2in1 落实、双真机组网预约、盘古/上传规则 |
| 文档/计划书官 | #9 计划书(收尾,等真机数据) |

详细任务书+可喂 AI 的角色提示词:`docs/分工与角色交接文档.md`

## 5. 技术速记(写代码前看)
- ArkTS 严格:无 any/as、字面量需类型、正则用 new RegExp、builder 内无语句
- 检查命令:`arkts_check`(逐文件)+ `build_project`(全量)
- 每轮改动跑单测(本地 DevEco 跑 List.test.ets,预期 50 绿)
- 布局常用:`Column/Row/List/ForEach`、mediaquery、`constraintSize({maxWidth})`、`wordBreak(WordBreak.BREAK_ALL)`、键盘 `setKeyboardAvoidMode(KeyboardAvoidMode.RESIZE)`(Index/KnowledgePage aboutToAppear 设、Disappear 回退)

## 6. 修订记录
| 日期 | 内容 |
|---|---|
| 2026-09-04 | 初版:截至 31a3bce(图文多选/键盘/气泡修复完成,50 单测,气泡修复用户确认 OK) |
